{
  "name": "gh-action-cache-clear",
  "version": "0.0.3",
  "description": "GitHub action to clear cache based on a pattern",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build script.js -o dist"
  },
  "author": "Open Control Systems",
  "license": "MPL 2.0",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.2"
  }
}
